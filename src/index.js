import todo from './todo.js'

// const folderId = todo.createFolder('folder 1') 
// const folderId2 = todo.createFolder('folder 2')
// todo.createFolder('folder 3')
// //todo.deleteFolder(folderId)
// todo.setActiveFolder(folderId)
// todo.setActiveFolder(folderId2)
// todo.createTask(folderId, '1')
// const taskId = todo.createTask(folderId, '2')
// todo.createTask(folderId, '3')

// //todo.deleteTask(folderId, taskId)

// todo.editTask(folderId, taskId, 'a')
// todo.toggleTask(folderId, taskId)
// todo.toggleTask(folderId, taskId)
// todo.printTasks(folderId)